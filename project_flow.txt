Create a git repo
create code.py and add some code
install dvc
now i will init dvc 
we will store the data into S3, we will store locally for now but we can save data in a S3  on AWS 
now do "mkdir S3" on terminal
now we will have to tell dvc where our data bucket is, for this we can do,"dvc remote add -d myremote() S3(our bucket directory)" 
initally we were tracking data folder using git and we have to run a command for git to stop tracking
and let dvc track in
now we make changes in data by adding a new row into the data
then we will again dvc push
we now what i have to do is using dvc to use into
some usefull command
dvc commit
dvc push
dvc pull
dvc status